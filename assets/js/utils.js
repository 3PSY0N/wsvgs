// import slugify from "slugify";
// const inputSlug = document.getElementsByClassName('input-slug');
// const slug = document.getElementsByClassName('slug');
//
// inputSlug[0].addEventListener('keyup', () => {
//     slug[0].value = slugify(inputSlug[0].value, {lower: true, strict: true});
// })
